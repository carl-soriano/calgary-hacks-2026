# --- Local development only (do not commit .env) ---
# Optional: for AI image analysis when running `npm run dev`.
# Get a key: https://platform.openai.com/api-keys
VITE_OPENAI_API_KEY=

# --- Production (Vercel) ---
# Do NOT put your API key in the repo or in VITE_* (it would be exposed in the browser).
# On Vercel: Project → Settings → Environment Variables → add:
#   Name:  OPENAI_API_KEY
#   Value: sk-your-key-here
# The serverless function at /api/analyze-image uses this; the key never leaves the server.
